# DevFlow.Projects.Infrastructure

Implements Projects persistence with EF Core and Oracle.

It contains `ProjectsDbContext`, entity configurations, `ProjectRepository`, and migrations for `PROJECTS`, `PROJECT_MEMBERS`, and `PROJECT_TASKS`.
