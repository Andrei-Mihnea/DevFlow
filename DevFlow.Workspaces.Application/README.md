# DevFlow.Workspaces.Application

Reserved for workspace use cases, handlers, DTOs, repository abstractions, and validation.

It currently contains only the project definition. The next use case can be `CreateWorkspaceCommand`, followed by membership commands such as `AddWorkspaceMemberCommand`.
