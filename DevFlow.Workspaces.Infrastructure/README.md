# DevFlow.Workspaces.Infrastructure

Implements Workspaces persistence with EF Core and Oracle.

It contains `WorkspacesDbContext`, entity configurations, and migrations for `WORKSPACES` and `WORKSPACE_MEMBERS`. It owns only the Workspaces database container.
