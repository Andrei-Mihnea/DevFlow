# DevFlow.BuildingBlocks.Auth

Reserved for authentication primitives that are genuinely shared by multiple services, such as JWT claim names or token-validation helpers.

It currently has no implementation. Keep it free of service-specific business rules; Auth service domain logic belongs in `DevFlow.Auth.Domain`.
